import styled from 'styled-components';

const StyledCart = styled.div`
  position: absolute;

  ul,
  li {
    margin: 0;
    padding: 0;
  }

  li {
    list-style: none;
  }
`;

export default StyledCart;
